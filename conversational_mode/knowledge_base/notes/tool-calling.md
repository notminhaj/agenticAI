# tool calling

Tool calling is when an AI decides to use an extra tool, like a calculator or a web search, when it can't solve something by itself. It's like asking a friend for help or googling for answers.